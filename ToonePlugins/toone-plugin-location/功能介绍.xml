
1、定位
 
  function successCallback(position){
	console.log("position = " + position);
  
  } 
  
   function errorCallback(error){
	console.log("error = " + error);
  
  } 
  
  var params = {};
  
  navigator.location.getCurrentPosition(successCallback, errorCallback, params);
<resources>

    <!-- Base application theme. -->
    <style name="CustomPayDialog" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="mystyle" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/pingpp_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/pingpp_dialog_exit</item>
    </style>

</resources>
